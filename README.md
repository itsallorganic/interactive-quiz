# interactive-quiz
Index file = skeleton of the page
style file = everything looks good
script = right/wrong formating

when adding a new qbank, simply create a new file in qbanks folder. 
Open the index.html file and copy the line 16 and paste it under it and change the title to the new file title
